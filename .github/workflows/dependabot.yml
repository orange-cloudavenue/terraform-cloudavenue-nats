name: Dependabot
version: 2
updates:

on:
  pull_request:
    paths:
      - '**.tf'

- package-ecosystem: github-actions
  directory: "/"
  schedule:
    interval: daily

- package-ecosystem: terraform
  directory: "/"
  schedule:
    interval: daily